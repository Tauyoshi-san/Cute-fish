# ファンシーフィッシュ 開発引継ぎ書

## 次の作業場所

`C:\Users\mia_k\OneDrive\デスクトップ\codex\4th_game\ファンシーフィッシュ`

「初期作業保存」は現テイストの保管用スナップショットとして扱い、今後は変更しないこと。

## プロジェクト概要

魚へ餌を与え、餌の履歴と水槽環境によって進化させる、クリック／タップ操作中心のアクアリウムゲーム。魚が死ぬなどの強いネガティブ要素はなく、観賞用デジタルアクアリウムとしても楽しめる設計。

## 次回の最優先作業

イラストと画面全体を「ファンシーフィッシュ」の新しい方向性へ刷新する。

1. 新アート方向性のラフを複数案作る
2. 水槽背景、魚、装飾、UIの共通デザインルールを決める
3. 承認された方向性で素材を差し替える
4. 素材変更後にサイズ、当たり判定、重なり、アニメーションを再調整する

既存画像を基準に描き足すのではなく、大幅なテイスト変更を前提に検討する。ゲームロジックはできる限り維持する。

## 実装済み

- Phaser 3 + TypeScript + Viteによるブラウザゲーム
- Windowsでダブルクリック起動できる `ゲームを起動.cmd` と `START_GAME.cmd`
- 水槽観察画面、HUD、ショップ、図鑑、掃除、リワード広告のデモ
- 餌購入、餌やり、魚購入・売却、進化条件、セーブ
- 魚の継続遊泳、複数の移動パターン、魚体モーション
- 水面、光、泡、ろ過水流のアニメーション
- 進化時のカメラ接近、タメ、演出
- 装飾の購入、ドラッグ配置、拡大縮小、回転、左右反転、前後移動、収納・再配置
- 装飾9種類（水草2、石、流木、結晶、石のアーチ、エアレーター、フィルター、ライト）

## 主なファイル

- `src/main.ts`：ゲーム起動
- `src/AquariumScene.ts`：Phaser水槽背景と水中表現
- `src/fishOverlay.ts`：魚表示、遊泳、進化演出
- `src/decorationOverlay.ts`：装飾の表示と配置編集
- `src/ui.ts`：HUD、ショップ、図鑑、餌やりUI
- `src/store.ts`：状態管理、購入、進化、セーブ
- `src/data.ts`：餌、魚種、ショップ商品
- `src/style.css`：画面全体のスタイルとアニメーション
- `public/assets`：現在の画像素材

## 起動方法

`ゲームを起動.cmd` をダブルクリックする。依存関係がなければランチャーが導入し、ローカルサーバーとブラウザを起動する。手動確認は `npm run build`。

## 注意事項

- セーブキーは現在 `aqua-alter-save-v3`。
- 魚と装飾はDOMオーバーレイで描画し、水槽背景と一部エフェクトはPhaser/CSSで描画。
- 装飾画像の下端が配置基準点。底砂へ置けるようY座標は画面高の96%まで許可。
- 収納中の装飾は進化条件へ加算しない。
- 現在の初期水槽は装飾なしを意図している。
- `dist`、`node_modules`、`.npm-cache` はコピー対象外。新フォルダでランチャーを実行すれば再生成される。
- ビルドは引継ぎ時点で成功。Viteのチャンクサイズ警告のみあり、動作を妨げない。

## デザイン刷新時の確認項目

- 可愛い魚として目、輪郭、ヒレ、陰影の統一
- 通常魚から幻想生物へ進化しても同一世界に見えるルール
- 背景と個別素材の光源・彩度・線密度の統一
- 魚が背景へ埋もれないコントラスト
- フィルターとライトは水槽設備として自然な設置位置に限定するか検討
- PCとスマートフォン双方でUIが水槽を隠しすぎないこと
- すべてオリジナルデザインとし、既存作品固有の意匠を模倣しないこと
