# ファンシーフィッシュ 正式リリース調整 引継ぎ書

更新日：2026-08-14

## 作業範囲（最重要）

- 作業対象は必ず `C:\Users\mia_k\OneDrive\デスクトップ\codex\4th_game\ファンシーフィッシュ` に限定する。
- `初期作業保存` フォルダは保管用スナップショットであり、変更禁止。
- 作業開始時に本書と `ゲーム仕様書.md` を全文読む。
- 既存のユーザー変更や未追跡ファイルを勝手に削除・巻き戻ししない。
- 完全オリジナルのデザインを維持し、既存作品固有のキャラクター・意匠を模倣しない。

## 現在地

ゲーム機能は本リリース前の最終調整へ進める状態。主要ループ、3段階進化、ショップ、図鑑、配置編集、背景・底砂切替、ミッション、音、セーブ、魚とのタップ交流まで実装済み。

次のタスクの目的は、新機能を無制限に増やすことではなく、正式UI調整、ゲームバランス調整、回帰確認、リリース品質への引き上げ。

直近の基準コミット：`237abb6 Rush and circle around aquarium taps`

## 最終調整の推奨優先順位

### A. UI正式調整

1. PC（標準、横長、低めの画面）とスマートフォン（820px以下、520px以下）のレイアウト確認
2. 右側の水槽一体型コンソールの余白、文字サイズ、スクロール量、押下領域の統一
3. ショップの餌・底砂・背景・装飾の情報密度と価格表示の最終整理
4. 魚ダイアログ、図鑑、ミッション、配置ツールバーの重なり・見切れ確認
5. 色覚、文字コントラスト、フォーカス表示、タップ領域の最低限のアクセシビリティ確認

### B. バランス調整

1. 初期120コインから最初の購入までの体験
2. ミッション報酬、掃除報酬、広告デモ報酬、魚売却額、商品価格の収支
3. 通常進化とレア進化に必要な餌代・装飾代・所要時間
4. 水質低下、空腹、幸福度、汚れ発生間隔の実プレイ確認
5. 魚最大8匹・装飾多数時の視認性と操作性

### C. リリースQA

1. 新規セーブと既存セーブ移行
2. 全13種の図鑑表示と進化ルート
3. 全商品購入、再選択、再起動後の保持
4. 装飾の全レイヤー、移動、拡縮、回転、反転、収納、復帰
5. BGM・効果音・音量・ミュート・ブラウザの自動再生制限
6. `npm run build`、起動用CMD、主要ブラウザでのスモークテスト

## 実装構成

- Phaser 3 + TypeScript + Vite。
- Phaser：水槽背景、背景切替、基本的な水中描画。
- DOM/CSS：魚、装飾、底砂、泡、汚れ、コンソール、ダイアログ、演出。
- 魚と装飾をPhaser側へ重複描画しないこと。

主なファイル：

- `src/main.ts`：アプリ起動、Phaser設定
- `src/AquariumScene.ts`：水槽背景の読込・切替
- `src/fishOverlay.ts`：魚の表示、遊泳、感情、餌演出、進化演出、タップ交流、泡生成
- `src/decorationOverlay.ts`：装飾の配置編集とレイヤー制御
- `src/careCycle.ts`：汚れ表示、30秒周期の時間更新
- `src/ui.ts`：コンソール、ショップ、図鑑、魚ダイアログ、音量UI
- `src/store.ts`：ゲーム状態、セーブ移行、購入、給餌、進化、掃除
- `src/progression.ts`：チュートリアル、ランダムミッション
- `src/audio.ts`：BGM、サンプル効果音、簡易生成音
- `src/data.ts`：餌、魚種、底砂、背景、装飾の商品データ
- `src/types.ts`：永続状態とIDの型
- `src/style.css`：全UI、レスポンシブ、演出、レイヤー
- `public/assets`：正式に参照している画像・音素材

## 重要なレイヤー仕様

奥から順に概ね以下。

1. 選択中の水槽背景（Phaser）
2. 後方の泡・光
3. 砂利より奥へ送った装飾（`z <= -10`）
4. 底砂
5. 魚より奥の装飾（`-9 <= z <= 0`）
6. 魚
7. 魚より手前の装飾（`z > 0`）
8. 汚れ
9. 前方の泡
10. 配置ツール、コンソール、ダイアログ、進化・ミッション演出

レイヤー調整時は次を守る。

- 砂利裏の装飾も配置編集時には再選択できること。
- 前方の泡は操作を奪わない（`pointer-events:none`）。
- 配置編集時は水槽タップによる魚呼び寄せを発生させない。
- 魚、装飾、汚れの直接クリックはそれぞれの既存操作を優先する。

## 素材制作ルール

- 魚：大きな統一アイ、濃紺の輪郭、明るい丸みのあるトゥーン表現。
- 第3段階：単純拡大ではなく、体格・尾・ひれ・外形を大きく変える。
- 装飾：画像の外周、とくに下端に不要な透明余白を作らない。下端が設置基準。
- 底砂：水槽幅を満たす高解像度、上端は滑らか、装飾を置ける低い凹凸、下端は画面外へ自然に見切れる。
- 背景：魚・底砂・前景装飾を含めず、下部約25%は底砂を重ねられる空間。全商品は同じアスペクト比。
- ショップの餌・装飾サムネイルは実物アセットと一致させる。
- 新しいラスタ素材は `imagegen` スキルの手順で生成・検品し、最終ファイルを必ず `public/assets` に保存する。

## 保存と互換性

- メインセーブキー：`aqua-alter-save-v4`
- 現在のスキーマ：`SAVE_VERSION = 9`
- ミッション／チュートリアル：`fancy-fish-progress-v2`
- 音設定は `src/audio.ts` の専用キー。
- セーブ更新では既存魚・購入品・装飾配置・発見済み図鑑を保持する。
- 新IDを追加する場合は型、許可ID一覧、データ、アセット対応、移行処理を同時に更新する。
- セーブ初期化はユーザーが明示した場合のみ行う。

## 起動・確認

- Windows：`ゲームを起動.cmd` または `START_GAME.cmd`
- 開発：`npm run dev`
- ビルド：`npm run build`
- 配布候補確認：`npm run preview`

現状、ビルドは成功。Viteの500kB超チャンク警告がある。動作を止める警告ではないが、本リリース前に初期ロード時間を測定し、必要ならコード分割を検討する。

## 既知の技術的注意点

- 自動テストは未整備。数値変更はブラウザで回帰確認すること。
- `store.ts`、`ui.ts`、`fishOverlay.ts` は一部が長い一行実装。大規模整形は差分を膨らませるため、正式調整と分離して行う。
- オフライン進行は最大12時間分に制限。
- 背景・魚アセットが大きく、JSもPhaserを含むため配信サイズの計測が必要。
- リワード広告は実SDKではなくデモUI。
- 真珠は表示・保存されているが、現時点で主要な消費先は未実装。リリース仕様として残すか隠すかを最終判断する。
- 魚の死やロストはなく、空腹・低水質でも最低値で止まる穏やかな設計。

## 完了条件

- 仕様書と実装の数値が一致している。
- 新規／移行セーブの双方で主要機能が動く。
- PCとスマートフォンでコンソールが操作でき、水槽の主要領域が確保される。
- 全商品・全魚・全レイヤーが欠損なく表示される。
- ビルド成功、重大なコンソールエラーなし、主要操作にブロッカーなし。
